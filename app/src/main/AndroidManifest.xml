<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dawn.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--&lt;!&ndash;value的值填写你在友盟后台申请的应用Appkey&ndash;&gt;-->
        <!--<meta-data android:value="53b276a556240b01840250ca" android:name="UMENG_APPKEY"/>-->
        <!--&lt;!&ndash;value的值填写渠道名称，例如yingyongbao。这里设置动态渠道名称变量&ndash;&gt;-->
        <!--<meta-data android:value="${UMENG_CHANNEL_VALUE}" android:name="UMENG_CHANNEL"/>-->
    </application>

</manifest>